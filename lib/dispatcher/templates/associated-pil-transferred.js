const value = `Someone associated with your establishment has changed the primary establishment where their licence is held.

You can view their updated licence:

{{{licenceUrl}}}`;

module.exports = {
  value,
  requires: ['licenceUrl']
};
