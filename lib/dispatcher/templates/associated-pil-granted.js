const value = `Someone associated with your establishment has had an application for a personal licence approved.

The licence is held at another establishment, but you can still view the granted licence:

{{{licenceUrl}}}`;

module.exports = {
  value,
  requires: ['licenceUrl']
};
